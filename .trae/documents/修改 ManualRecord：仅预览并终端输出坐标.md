## 目标
- 取消保存图片与坐标到文件，仅在终端打印坐标。
- 保持摄像头预览窗口正常显示，用户可通过按键触发打印。

## 文件与现状
- 目标文件：e:\DualArm\Magician\ManualRecord.py
- 关键逻辑：
  - 保存函数：见 [ManualRecord.py:L54-L69](file:///e:/DualArm/Magician/ManualRecord.py#L54-L69)，含 `cv2.imwrite` 和坐标文件写入，同时打印坐标。
  - 主循环：见 [ManualRecord.py:L108-L162](file:///e:/DualArm/Magician/ManualRecord.py#L108-L162)，负责摄像头帧获取、运动检测与 `cv2.imshow` 显示，并通过按键触发保存。

## 改动点
- 在保存函数中：
  - 移除或注释所有图片保存调用（`cv2.imwrite`）。
  - 移除或注释坐标写入文件（如 `open(...).write(...)`）。
  - 保留并规范化坐标打印（例如：`print(f"Pose: {x:.3f} {y:.3f} {z:.3f} {rx:.3f} {ry:.3f} {rz:.3f}")`）。
- 在主循环中：
  - 将原“保存”按键（如 `s`）改为“打印坐标”触发，仅调用打印函数，不进行任何磁盘写入。
  - 保留 `cv2.imshow` 预览窗口与状态叠加（如在画面上显示当前坐标）。
  - 保留退出按键（如 `q`）。

## 交互与输出
- 预览窗口实时显示摄像头画面，可叠加文本提示（当前坐标或就绪状态）。
- 按 `s` 时，终端打印当前机械臂末端位姿坐标（不保存图片）。
- 坐标输出为单行、易解析的格式（支持后续采集）。

## 验证
- 运行脚本后应：
  - 成功打开摄像头画面。
  - 按 `s` 仅在终端打印坐标，不生成任何图片或坐标文件。
  - 按 `q` 正常退出，无异常日志与残留文件。

## 变更范围最小化
- 不改动摄像头索引、分辨率、运动检测与位姿更新线程逻辑。
- 仅移除磁盘写入相关代码，保留功能路径与调用关系。

## 预计修改行
- 保存函数：移除写盘调用与文件写入，保留 `print`（参考 [ManualRecord.py:L54-L69](file:///e:/DualArm/Magician/ManualRecord.py#L54-L69)）。
- 主循环键盘事件：将“保存”替换为“打印”，确保 `cv2.imshow` 仍在（参考 [ManualRecord.py:L108-L162](file:///e:/DualArm/Magician/ManualRecord.py#L108-L162)）。

请确认以上方案，确认后我将按上述步骤实施修改并验证。